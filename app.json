{
  "name": "predictionio-engine-classification",
  "description": "Heroku PredictionIO Engine",
  "keywords": [
    "machinelearning",
    "predictionio"
  ],
  "repository": "https://github.com/gongsy945/predictionio-engine-classification",
  "env": {
    "PIO_EVENTSERVER_ACCESS_KEY": {
      "required": true
    },
    "PIO_EVENTSERVER_APP_NAME": {
      "required": true
    },
    "PIO_EVENTSERVER_HOSTNAME": {
      "required": true
    },
    "PIO_EVENTSERVER_PORT": {
      "required": true
    }
  }
}
